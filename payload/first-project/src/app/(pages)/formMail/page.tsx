import { sentDoc } from "../../_api/sendMail"
import { UpdateLayoutPayload } from "../../_api/functionUpdateGlobal";
export default async function formMail({ searchParams }: any) {
  // sentDoc();
  // UpdateLayoutPayload("heading", [], [])
  return (
    <>
      <p>hello user</p>
    </>
  )
}
